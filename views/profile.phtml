<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>User Profile</h1>
    <?php
    if (isset($_SESSION['user']) && $_SESSION['user'] !== false) {
        $user = $_SESSION['user'];
        echo '<p><strong>Username:</strong> ' . htmlspecialchars($user->getUsername()) . '</p>';
        echo '<p><strong>Email:</strong> ' . htmlspecialchars($user->getEmail()) . '</p>';
        echo '<p><a href="index.php?c=orders">View Order History</a></p>';
    } else {
        echo '<p>Please log in to view your profile.</p>';
    }
    ?>
    <br>
    <a href="index.php">← Back to shop</a>
</body>
</html>
